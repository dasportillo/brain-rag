node_modules/

# Local index + runtime artifacts (never commit — contains your conversation data)
brain.db
brain.db-wal
brain.db-shm
keep.list
*.log

# Eval artifacts (contain retrieved transcript text)
eval-cases.local.json
eval-bundle.json
verdicts.json

# Per-project curated state notes (contain your work details)
state/

# Embedding model cache
.cache/
models/

# OS
.DS_Store
